    //For de iteracciones
   export const procesoPesado= (iteraciones) =>{
        for(let i=0; i<iteraciones ;i++ ){

            console.log('Haí vamos')
        }
     
        return `${iteraciones} iteracciones realizadas.`
    }
    //-----------------